import React from 'react';
// eslint-disable-next-line
import Select from 'ringcentral-widget/components/Select';

const props = {};
props.options = [];

/**
 * A example of `Select`
 */
const SelectDemo = () => (
  <Select
    {...props}
  />
);
export default SelectDemo;
