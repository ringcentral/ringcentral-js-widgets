import React from 'react';
// eslint-disable-next-line
import EntityButton from 'ringcentral-widget/components/EntityButton';

const props = {};

/**
 * A example of `EntityButton`
 */
const EntityButtonDemo = () => (
  <EntityButton
    {...props}
  />
);
export default EntityButtonDemo;
