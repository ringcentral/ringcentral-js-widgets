import React from 'react';
// eslint-disable-next-line
import RecipientsHeader from 'ringcentral-widget/components/RecipientsHeader';

const props = {};
props.currentLocale = 'en-US';

/**
 * A example of `RecipientsHeader`
 */
const RecipientsHeaderDemo = () => (
  <RecipientsHeader
    {...props}
  />
);
export default RecipientsHeaderDemo;
