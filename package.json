{
  "private": true,
  "devEngines": {
    "node": "8.x || 9.x"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "lerna run test --stream",
    "start": "lerna run start --scope ringcentral-widgets-demo --stream",
    "release": "lerna run release --scope ringcentral-widgets ringcentral-integration"
  },
  "devDependencies": {
    "lerna": "^2.9.0"
  }
}
