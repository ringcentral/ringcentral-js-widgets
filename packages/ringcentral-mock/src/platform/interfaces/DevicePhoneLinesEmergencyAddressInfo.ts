export interface DevicePhoneLinesEmergencyAddressInfo {
  /**
   * 'True' if specifying of emergency address is required
   */
  required: boolean;
  /**
   * 'True' if only local emergency address can be specified
   */
  localOnly: boolean;
}
