// For 'Audio' mode only. Prompt media reference
export interface AudioPromptInfo {
  /**
   * Link to a prompt audio file
   */
  uri: string;
  /**
   * Internal identifier of a prompt
   */
  id: string;
}
