export interface TransferExtensionInfo {
  /**
   * Internal identifier of an extension the call is transferred to
   */
  id: string;
  /**
   * Extension name
   */
  name: string;
  /**
   * Extension number
   */
  extensionNumber: string;
}
