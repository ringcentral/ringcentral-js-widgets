export interface CallerIdDeviceInfo {
  /**
   * Internal identifier of a device
   */
  id: string;
  /**
   * Link to a device resource
   */
  uri: string;
  /**
   * Name of a device
   */
  name: string;
}
