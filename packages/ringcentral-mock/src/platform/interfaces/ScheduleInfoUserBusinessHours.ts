import { WeeklyScheduleInfo } from './WeeklyScheduleInfo';

// Schedule when an answering rule is applied
export interface ScheduleInfoUserBusinessHours {
  /**
   */
  weeklyRanges: WeeklyScheduleInfo;
}
