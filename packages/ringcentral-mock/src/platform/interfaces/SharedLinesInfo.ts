// SharedLines call handling action settings
export interface SharedLinesInfo {
  /**
   * Number of seconds to wait before forwarding unanswered calls. The value range is 10 - 80
   */
  timeout: number;
}
