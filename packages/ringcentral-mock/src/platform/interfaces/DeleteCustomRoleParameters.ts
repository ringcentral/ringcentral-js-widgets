// Query parameters for operation deleteCustomRole
export interface DeleteCustomRoleParameters {
  /**
   * Specifes that role should be validated prior to deletion whether it can be deleted or not
   */
  validateOnly: boolean;
}
