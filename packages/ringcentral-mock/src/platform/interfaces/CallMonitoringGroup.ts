export interface CallMonitoringGroup {
  /**
   * Link to a call monitoring group resource
   */
  uri: string;
  /**
   * Internal identifier of a group
   */
  id: string;
  /**
   * Name of a group
   */
  name: string;
}
