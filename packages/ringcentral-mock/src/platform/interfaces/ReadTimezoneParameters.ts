// Query parameters for operation readTimezone
export interface ReadTimezoneParameters {
  /**
   * Indicates the page number to retrieve. Only positive number values are accepted
   * Default: 1
   */
  page: string;
  /**
   * Indicates the page size (number of items)
   * Default: 100
   */
  perPage: string;
}
