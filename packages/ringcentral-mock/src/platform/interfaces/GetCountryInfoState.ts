// Information on a country the state belongs to
export interface GetCountryInfoState {
  /**
   * Internal identifier of a state
   */
  id: string;
  /**
   * Canonical URI of a state
   */
  uri: string;
}
