export interface PagingDeviceInfo {
  /**
   * Internal identifier of a paging device
   */
  id: string;
  /**
   * Link to a paging device resource
   */
  uri: string;
  /**
   * Name of a paging device
   */
  name: string;
}
