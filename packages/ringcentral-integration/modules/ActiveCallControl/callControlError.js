import Enum from '../../lib/Enum';

export default new Enum(
  [
    'holdConflictError',
    'unHoldConflictError',
    'muteConflictError',
    'unMuteConflictError',
    'generalError',
  ],
  'callControl',
);
